{% extends 'base.html.twig' %}

{% block title %}{{ canal.nombreCanal }}{% endblock %}

{% block body %}
    <h1>{{ canal.nombreCanal }}</h1>

    {% if app.user %}
        <button type="button" class="btn btn-primary">Ha iniciado sesión</button>
        <a href="{{ path('video_new') }}" data-toggle="modal" data-target="#exampleModal">Subir vídeo</a>
            {% if esTuCuenta is defined %}
                <button type="button" class="btn btn-primary">Personalizar canal</button>
                <button type="button" class="btn btn-primary">Subir vídeo</button>
            {% endif %}
            <!-- Button trigger modal -->
        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
            Launch demo modal
        </button>

        <button type="button" id="buttonPrueba">ButtonPrueba</button>
        

         <!-- Modal -->
        <div class="modal fade " id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content bg-dark">
                    <div class="modal-header">
                    <h5 class="modal-title text-white" id="exampleModalLabel"> Subida de vídeo</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    </div>
                    <div class="modal-body">
                        {#{% include "video/new.html.twig" with {'form': form, 'video': video} %}#}
                        {{ render(path('video_new')) }}
                        {#{{ render(controller(
                            'App\\Controller\\VideoController::new'
                        )) }}#}
                    </div>
                </div>
            </div>
        </div>
        {% endif %}
        {% include "video/index.html.twig" with {'videos': canal.videos} %}
       
            
    

    <!--<a href="{{ path('canal_index') }}">back to list</a>

    <a href="{{ path('canal_edit', {'id': canal.id}) }}">edit</a>

    {{ include('canal/_delete_form.html.twig') }}-->
{% endblock %}
